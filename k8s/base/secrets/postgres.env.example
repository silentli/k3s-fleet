# Replace the password in both values. URL-encode special characters in the URL.
POSTGRES_PASSWORD=replace-with-a-strong-password
DATABASE_URL=postgresql+psycopg://fleet:replace-with-a-strong-password@postgres:5432/fleet
