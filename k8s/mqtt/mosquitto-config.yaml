apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-config
  labels:
    app: mosquitto
data:
  mosquitto.conf: |
    listener 1883
    # Allow anonymous access only for temporary testing and development
    allow_anonymous true
    persistence true
    persistence_location /mosquitto/data/
